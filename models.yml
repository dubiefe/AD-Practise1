Person:
  required_vars:
    - email
    - name
  admissible_vars:
    - address
    - interests
    - skills
    - company_email
    - educational_center_email
  unique_indexes:
    - email
  regular_indexes:
    - name
  location_index: address
Company:
  required_vars:
    - email
    - name
    - address
  unique_indexes:
    - email
  regular_indexes:
    - name
  location_index: address
Educational_Center:
  required_vars:
    - email
    - name
    - address
  unique_indexes:
    - email
  regular_indexes:
    - name
  location_index: address
