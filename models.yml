User:
  required_vars:
    - email
    - name
  admissible_vars:
    - age
    - address
    - interests
    - skills
    - company_email
    - educational_center_email
  unique_indexes:
    - name
  regular_indexes:
    - email
  location_index: address
Company:
  required_vars:
    - email
    - name
    - address
  unique_indexes:
    - name
  regular_indexes:
    - email
  location_index: address
Educational_Center:
  required_vars:
    - email
    - name
    - address
  unique_indexes:
    - name
  regular_indexes:
    - email
  location_index: address
