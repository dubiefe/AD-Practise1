User:
  required_vars:
    - email
    - name
  admissible_vars:
    - age
    - address
    - interests
    - skills
    - company_email
    - educational_center_email
  unique_indexes:
    - email
  regular_indexes:
    - name
  location_indexes: address
Company:
  required_vars:
    - email
    - name
    - address
  unique_indexes:
    - email
  regular_indexes:
    - name
  location_indexes: address
Educational_Center:
  required_vars:
    - email
    - name
    - address
  unique_indexes:
    - email
  regular_indexes:
    - name
  location_indexes: address
