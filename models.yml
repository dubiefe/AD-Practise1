Person:
  required_vars:
    - _id
    - nane
  admissible_vars:
    - address
    - interests
    - skills
    - company_id
    - educational_center_id
  unique_indexes:
    - _id
  regular_indexes:
    - name
  location_index: address
Company:
  required_vars:
    - _id
    - nane
    - address
  unique_indexes:
    - _id
  regular_indexes:
    - name
  location_index: address
Educational_Center:
  required_vars:
    - _id
    - nane
    - address
  unique_indexes:
    - _id
  regular_indexes:
    - name
  location_index: address

